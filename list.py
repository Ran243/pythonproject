numbers  = ["one", "two", "three"]
print(numbers[1])
#the first item in a list is 0
numbers.append("four")
#add item to end of array
print(numbers)
numbers.pop()
#removes a item from array, depends on number in (), if no input removes last item
print(numbers)
numbers.insert (0, "zero")
numbers.insert (4, "four")
#inserts number needs to know location first
print (numbers)
numbers = ['zero', 0, 'one', 1, 'two', 2, 'three', 3, 'four', 4]
#you can also use the numbers.insert() function
print(numbers)
#using a loop makes it easier to print each item of the array
count = 0
while count < len(numbers):
    print(numbers(count))
    count = count + 1
numbers = [
    "zero", "one", "two", "three"
    ["0", "1", "2", '3']]
print (numbers[2], [1])
#the function str finds the number of words in a list